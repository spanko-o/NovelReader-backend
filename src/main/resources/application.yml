spring:
  datasource:
    url: jdbc:mysql://localhost:3306/novelreader?useSSL=false&serverTimezone=UTC
    username: root
    password: Y1j1i1123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true


# 禁用 Spring Security 自动配置

  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration